import { AutoTranslateDirective } from './auto-translate.directive';

describe('AutoTranslateDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoTranslateDirective();
    expect(directive).toBeTruthy();
  });
});
